# ==============================================================================
# CartesianOS Project Gitignore
# Hardened to prevent Binary Leakage (Audit 4A) and tracking of build artifacts.
# ==============================================================================

# --- RUST (Cartesian Core) ---
# Ignore build artifacts and backups
src/cartesian-core/target/
**/*.rs.bk

# Audit 4A Fix: Specifically block compiled binaries in source roots
src/cartesian-core/main
src/cartesian-core/cartesian-core

# Cargo registry and git db (massive folders)
.cargo/registry/
.cargo/git/

# Note: We keep Cargo.lock for binaries to ensure reproducible builds.

# --- ARCH PACKAGING (pkg folder) ---
# Keep PKGBUILD and scripts, but ignore makepkg runtime folders
pkg/src/
pkg/pkg/
pkg/*.pkg.tar.zst
pkg/*.log

# --- LOCAL REPOSITORY ---
# Ignore the binary package repo we created
repo/

# --- ARCH ISO BUILDER ---
# Massive temporary build files
iso/work/
iso/out/
*.iso
iso/local_repo/
iso/tmp/
.archiso/

# Config backups created during build injection
*.bak
iso/archiso_profile/pacman.conf.bak

# --- PYTHON ---
# Generated by the build.sh HTTP server
**/__pycache__/
**/*.pyc
**/*.pyo

# --- IDE / EDITOR SETTINGS ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# --- VIRTUAL MACHINES ---
*.vdi
*.vbox
*.vbox-prev

# --- LOGS & DIAGNOSTICS ---
logs/
*.log

# --- GENERIC BINARY ARTIFACTS ---
*.so
*.a
*.o